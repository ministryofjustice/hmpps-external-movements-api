---
# Per environment values which override defaults in hmpps-external-movements-api/values.yaml

generic-service:
  ingress:
    host: external-movements-api.hmpps.service.justice.gov.uk

  env:
    SENTRY_ENVIRONMENT: prod
    INTEGRATION_HMPPS_AUTH_URL: "https://sign-in.hmpps.service.justice.gov.uk/auth"
    INTEGRATION_PRISONER_SEARCH_URL: "https://prisoner-search.prison.service.justice.gov.uk"

# CloudPlatform AlertManager receiver to route prometheus alerts to slack
# See https://user-guide.cloud-platform.service.justice.gov.uk/documentation/monitoring-an-app/how-to-create-alarms.html#creating-your-own-custom-alerts
generic-prometheus-alerts:
  alertSeverity: hmpps-alerts-api-prod
